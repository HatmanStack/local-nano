# Feedback Log

## Active Feedback

## Resolved Feedback

### CODE_REVIEW - Phase 7 [DOC-ENGINEER] - iteration 1

Status: RESOLVED

Resolution: Re-verified all three citations against HEAD and corrected the stale
anchors in commit `docs: correct three stale code-line citations against HEAD`:
`offscreen.ts:64-67` → `:76-79` (imports :77-78), `offscreen.ts:71` → `:83`, and
`prompt-api-polyfill.js:181` → `:189` (across `docs/prompt-api.md`,
`docs/architecture.md`, `docs/configuration.md`). Prose unchanged; markdownlint,
`lint:ci`, and the docs-config guard (17 tests) all green.

## Phase Approvals

- PHASE_APPROVED - Phase 1 [HYGIENIST] - iteration 1 - dead-code removal (system.ts, finalize no-op, dot_env.json, web-store zip); 205 tests pass, build/typecheck/lint clean.
- PHASE_APPROVED - Phase 2 [HYGIENIST] - iteration 1 - BUTTON_CSS extraction + console.log gating via src/debug.ts; 206 tests pass, build/typecheck/lint clean.
- PHASE_APPROVED - Phase 3 [IMPLEMENTER] - iteration 1 - offscreen listener consolidation (HIGH-1). `src/offscreen/dispatch.ts` classifyOffscreenMessage delegates to the three real protocol guards; the three sibling `onMessage.addListener` blocks collapsed into ONE dispatcher returning `true` only for owned messages and `false` for everything else (verified the exact MV3-race fix); handler bodies byte-equivalent to originals (git diff), count-tokens best-effort comment preserved, `onConnect` untouched. Exactly one `chrome.runtime.onMessage.addListener` and one `onConnect.addListener`. 216 tests pass (+10 dispatch cases asserting real behavior); typecheck/lint:ci/build clean, dist/offscreen.js emitted.
- PHASE_APPROVED - Phase 4 [IMPLEMENTER] - iteration 1 - stream-lifecycle extraction (HIGH-2) + storage hardening. `runStreamTurn(opts)` consolidates the triplicated sendChat/sendAsk/sendRewrite scaffold; reviewer traced all three paths line-by-line and confirmed behavior-preserving (recordSentTurn gating, AbortError path, empty-response fallback, finally cleanup all intact). `isEntry` per-element loadHistory validation; `isQuotaError` one-time quota advisory in persist() (non-quota errors still console.error). 223 tests pass (+7: 4 history + 3 session); typecheck/lint:ci/build clean, coverage above gate.
- PHASE_APPROVED - Phase 5 [IMPLEMENTER] - iteration 1 - eval remediation (5 targets). 5.1 pageContext slices `limit*8` raw window before whitespace collapse then caps; +2 tests prove cap and that collapse still applies inside the bounded window (100% file coverage). 5.2 BusyGate (new `src/offscreen/busy-gate.ts`) reject-when-busy; offscreen `tryAcquire()` is checked BEFORE allocating the controller/read-loop so a second concurrent stream posts `StreamDone {ok:false, busy}` without starting a second `promptStreaming`; `release()` sits in the read-loop `finally` covering success/error/abort, and `onDisconnect` aborts the controller so the finally runs on disconnect; single-shared-session singleton (`ensureSession`/`sessionPromise`) untouched (ADR-R2). 5.3 vendored abort threading is additive + degrade-safe (ADR-R4): no-signal path yields a byte-identical options object (null criterion spreads `{}`); `StoppingCriteria` absence guarded by `typeof !== 'function'` + try/catch returning null (never throws); `_call` returns `input_ids.map(()=>!!signal.aborted)` so decode halts once aborted; both edits commented LOCAL DELTA with Phase-7 resync notes. Verified the installed `@huggingface/transformers ^4.2.0` exports `StoppingCriteria` (function, `_call` on prototype) and accepts `stopping_criteria` per `parameters.d.ts`, so the commit-body API claim holds. 5.4 `get contextWindow()` returns `131072` (not 1M, not 8-32k) with LOCAL DELTA comment; bundle contains `131072` (x4) and zero `1000000`; the remaining 1M literals live only in the static `create()` initial-prompts guard and WPT synthetic-detection scaffolding (instance runtime guards at :720/:921/:1067 read the getter) — leaving them is correct per ADR-R3's getter-only scope and is accurately flagged in the commit body. 5.5 `restore()` awaits `rebuildSession(seed)` with system entries dropped via a typed filter, guarded in try/catch that degrades to render-only (no throw), `isFirstTurn` stays `true` so the first new turn still prefixes current-page context; NOTE(isFirstTurn) comment updated; +3 tests (seed maps user/model only, no seed when empty, rejection degrades to render-only). 5.6 dispatch.ts + busy-gate.ts at 100% coverage. Model name `gemma-4-E2B-it-ONNX` unchanged. 234 tests pass (+11: 2 pageContext + 5 busy-gate + 1 client + 3 session, all real); typecheck/lint:ci/build clean; coverage exit 0 (busy-gate/dispatch/pageContext 100%, session.ts 97.24%, branches 84.63%); commits atomic + conventional, working tree clean.
- PHASE_APPROVED - Phase 6 [FORTIFIER] - iteration 1 - guardrails. 6.1 removed `noExplicitAny: "off"` from biome.json (recommended:true governs it) + ONE scoped `// biome-ignore` at tests/setup.ts:115; reviewer empirically confirmed an injected `any` in src/ is flagged and the ignore is the only exception. 6.2 CI setup-node uses `node-version-file: .nvmrc`. 6.3 bidirectional docs/testing.md drift guard in tests/docs-config.test.ts (every tests/*.test.ts in table + no stale entries); reviewer confirmed both drift directions fail as expected. Coupling: the testing.md table fix (7→15 entries) landed in the same commit (ccc2df4) to keep the suite green, sanctioned by the plan; guard checks filenames only so Phase 7 can still refine descriptions. 250 tests pass (+16); typecheck/lint:ci/build clean, coverage above gate. Commits c9409a3, ed33961, ccc2df4.
- PHASE_APPROVED - Phase 7 [DOC-ENGINEER] - iteration 2 - documentation fixes. 10 docs corrected against current code. Iteration 1 CHANGES_REQUESTED for 3 stale line-citations copied from pre-remediation audit numbers (the same unverified-claim pattern that triggered this re-audit); iteration 2 fixed them against HEAD (offscreen.ts:76-79, :83; polyfill:189) in commit 1c8c6d0, no stale :64-67/:71/:181 remain, prose byte-identical. Corrections respected (model name gemma-4 unchanged, apiKey polyfill-read wording, context window 131072). 250 tests / lint:ci / docs-config guard green.

## Verification

- VERIFIED - Stage 3 - 2026-05-23. All real findings from eval.md / health-audit.md / doc-audit.md confirmed addressed across the 7 phases; corrected/withdrawn findings (model name, web-store git-tracking, apiKey removal, npm-audit, 8-32k context window) correctly counted as not-applicable. Gates all green: npm test 250 passing (15 files), typecheck clean, lint:ci clean, build emits background/content/offscreen.js, coverage 93.35% stmts / 84.63% branches (above 75/80 gate; busy-gate/dispatch/pageContext/history at 100%, session.ts 97.24%). HIGH-1/2/3 verified; all six eval pillars' targets verified (abort-threading + busy-gate verified by reading vendored code, not runtime, as they don't load under Vitest). Health LOW-17 (collectGpuInfo optimistic isFallback) left as-is — never a remediation target, accepted original state, not a regression.
