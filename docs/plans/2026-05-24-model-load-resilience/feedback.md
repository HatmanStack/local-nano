# Feedback Log

This file is the channel between the Plan Reviewer and the Planning Architect.
Reviewer entries are tagged `PLAN_REVIEW`. The architect resolves each item,
moves it to Resolved with a short note on what changed, and the implementer
treats Resolved items as binding context.

## Active Feedback

_None yet._

## Resolved Feedback

_None yet._

## Phase Approvals

- PHASE_APPROVED - Phase 1 (lifecycle gate) - iteration 1 - terminal-crash classifier (`src/offscreen/failure.ts`), force-recreate SW path (`recreateOffscreen` via closeOffscreen+ensureOffscreen, no `hasDocument()` dependency, swallows crashed-doc closeDocument rejection), proactive at-warmup terminal UI + manual Retry replacing the silent warmup swallow, minimal copy-only diagnostic (`src/offscreen/diagnostic.ts`, activeTier null until Phase 2). No vendor edits, single shared session preserved. Replaced the old "degrades silently on warmup failure" test (asserted the removed contract) with real terminal-UI/Retry tests. 293 tests pass (+43), typecheck/lint:ci/build clean, coverage above gate (failure.ts + diagnostic.ts 100%).
- PHASE_APPROVED - Phase 2 (fallback ladder + tier persistence) - iteration 1 - verified against Phase-0 ADRs and Phase-2 spec with tool evidence:
  1. Single session / no overlap (the v0.2.0 OOM guard): the `ensureWarm` loop in `src/session.ts` (lines 904-935) always calls `recreateOffscreen()` BEFORE attempting the next tier on a load failure, tearing down the GPU generator per ADR-R3/R4; `handleWarmup` in `offscreen.ts` (lines 295-335) destroys any prior `sessionPromise` and nulls it before `ensureSession()` when a different tier is requested. No path can have two `LanguageModel.create()` live at once. Test "recreates the document between every failed rung and never overlaps loads" asserts 4 attempts / exactly 3 recreates.
  1. No vendor edits: `git diff 1aeaca7..HEAD --name-only | grep vendor` is empty. Tier override is by setting `window.TRANSFORMERS_CONFIG` via the pure `applyTierToConfig` (ladder.ts), not by patching the polyfill.
  1. Auto-fallback on LOAD only: the stream/runtime onConnect handler in `offscreen.ts` (lines 363-470) is byte-identical to Phase 1 (the commit diff touches none of it); the guard-removed comment and keep-session-alive-on-stream-error behavior are intact. No mid-stream auto-rebuild.
  1. Ladder reducer correctness: `src/offscreen/ladder.ts` `nextAction`/`firstTierIndex` are pure; `PRIMARY_LADDER` is q4f16/q8/fp16/wasm with tier 0 = `.env.json` base. Tests (`tests/offscreen-ladder.test.ts`, 19 cases) assert advance-on-failure, success short-circuit, known-bad skipping, full-walk to exhausted, and firstTierIndex with/without known-good. ladder.ts 100% coverage.
  1. Persistence: `src/offscreen/capability-store.ts` uses the NEW global key `local-nano:capability:v1` (distinct from `local-nano:history:<origin><pathname>`); schema- and extension-version invalidation works; recordKnownGood drops a prior known-bad and recordKnownBad clears a stale known-good; clear uses `chrome.storage.local.remove`; the `FakeStorageArea.remove` addition mirrors the real API and is reset in `beforeEach`. Tests (`tests/capability-store.test.ts`, 10 cases) assert all of these including both invalidation paths.
  1. activeTier is now populated in the terminal diagnostic (session.ts lines 758-760); test asserts `activeTier: onnx-community/gemma-4-E2B-it-ONNX/wasm/q8` on full exhaustion.
  1. Build/tests/lint/coverage green: `npm test` 345 pass (+52, 293 to 345), `npm run typecheck` clean, `npm run lint:ci` clean (54 files), `npm run build` green, coverage 94.32% lines / 86.4% branches (above 75/80 gate). Four atomic conventional commits, no Co-Authored-By, no stray console.log/TODO/FIXME. Terminal bubble offers both Retry (skip known-bad) and Reset-and-re-detect (clear + re-walk) as specified.
- PHASE_APPROVED - Phase 3 (capability + model selection, #4) - iteration 1 - `src/offscreen/capability.ts` (classifyCapability + CAPABLE_MIN_BUFFER_BYTES = 1 GiB, wired into preflightWarning as a behavior-preserving literal→constant swap); `ladder.ts` extended with default-off `SMALLER_MODEL_ENABLED = false` + `assembleLadder({capability, smallerEnabled})` + candidate `onnx-community/Qwen2.5-0.5B-Instruct` (WASM-vetted per docs/models.md; the gemma-3-1b WASM trap is NOT used); ensureWarm wired via the `isSmallerModelEnabled()` seam. Reviewer independently verified model claims against docs/models.md. Flag OFF = behaviorally identical to Phase 2 (gemma-4-E2B for all, primary ladder only); `.env.json` default unchanged; no vendor/stream edits; single session preserved. Seams unit-tested flag on (spy) and off. 365 tests pass (+20), typecheck/lint:ci/build clean, coverage above gate (capability.ts + ladder.ts 100%).
- PHASE_APPROVED - Phase 4 (phased download progress, #3) - iteration 1 - pure `src/offscreen/progress.ts` reducer (clamp 0-100, monotonic, hold-on-bad-input); STREAM_PROGRESS_PORT + ProgressFrame relaying the polyfill's PUBLIC `monitor` downloadprogress offscreen→panel (verified monitor option at prompt-api-polyfill.js:439-470 — no vendor edits); phased panel UI (Downloading NN% → indeterminate "Loading into GPU…" at 100%, no parked bar; falls back to elapsed counter when no frames); distinct `network` failure class (classifyLoadFailure terminal-takes-precedence; network failure does NOT record known-bad / advance ladder / recreate — retries same tier). Single session + load-only preserved; progress frames are local port messaging (nothing leaves device). 402 tests pass (+37), typecheck/lint:ci/build clean, coverage above gate (progress.ts + failure.ts 100%).
- PHASE_APPROVED - Phase 5 (rich always-available copy-only diagnostic, #5, FINAL) - iteration 1 - verified against Phase-0 ADR-R11 and the Phase-5 spec with tool evidence:
  1. COPY-ONLY / NO EGRESS (the hard privacy constraint): grep of `src/offscreen/diagnostic.ts` and the copy/diagnostic paths in `src/session.ts` finds no `fetch`, `XMLHttpRequest`, `WebSocket`, `sendMessage`, beacon, or telemetry (the two `fetch` tokens in session.ts are comment prose about the HF weights download). `copyToClipboard` (session.ts) is the ONLY side effect: it prefers `navigator.clipboard.writeText` and falls back to a hidden off-screen textarea + `document.execCommand('copy')`, both try/catch-wrapped to resolve false rather than throw. The test "does not send the diagnostic over the network (no fetch, no sendMessage)" spies `globalThis.fetch` and snapshots `chrome.runtime.sendMessage.mock.calls.length`, asserting neither fires on copy. Nothing auto-sends.
  1. NO SECRETS: `DiagnosticInput` carries only device/error metadata (device, isFallback, maxBufferSize, chosenModel, activeTier, ladderPath of model/device/dtype + outcome, errorClass+message, extensionVersion, parsed chromeVersion, raw userAgent). No prompt text, page content, chat history, or tokens; grep of diagnostic.ts for prompt/pageContent/history/token finds only comment matches. privacy.md states it "holds no prompts, page content, or chat history."
  1. PURE SERIALIZER SEAM: `buildDiagnostic` and `parseChromeVersion` are pure, no Chrome import; `tests/offscreen-diagnostic.test.ts` (+9 cases) covers chosenModel (and null→n/a), Chrome/Edge-on-Chromium/non-Chrome UA parsing, ladder-path rendering with all three outcomes (success/load-failure/network), and the empty-path `none` token. Clipboard is the only mocked side effect in session tests. Real assertions, not placeholders.
  1. NO VENDOR EDITS: `git diff 34359b7~1..HEAD --name-only | grep vendor` is empty (touched files: diagnostic.ts, session.ts, two test files, configuration.md, privacy.md). No new `LanguageModel.create` introduced; single shared session preserved.
  1. ALWAYS-AVAILABLE: `root.appendChild(makeCopyDiagnosticAffordance())` runs unconditionally at `initSession` time (session.ts ~line 1202), not inside any failure branch; the affordance is `position: absolute` anchored to the `position: fixed` panel root (content.ts line 25). On click it rebuilds from CURRENT live state via `buildDiagnosticInput(lastWarmError)` (lastGpuInfo, current ladderPath/chosenModel, last warm error or `none`); a successful load clears `lastWarmError`. Tests assert presence right after init with no failure, and that a post-terminal-failure copy carries the live error + walked path.
  1. COMMIT HYGIENE: four conventional commits matching the spec templates, no Co-Authored-By/Generated-By/emoji (grep clean). The `git reset --soft` recombination of builder (5.1) + its call sites (5.2) lands in commit 34359b7, whose body documents that updating the call sites was required to keep the build green against the widened input — atomic-per-logical-change, no `--amend` artifacts, linear history. None of the four commits are on any remote (`git branch -r --contains` per-commit empty; `web-store` has no upstream), so no published commit was rewritten.
  1. FEATURE COMPLETE / GREEN: `npm test` 419 pass (+17, 402 to 419), `npm run typecheck` clean, `npm run lint:ci` clean (58 files), `npm run build` green, coverage 94.99% lines / 87.64% branches (above 75/80 gate; diagnostic.ts 100/95, session.ts 97/93). Docs accurate (configuration.md "Copy diagnostic" section + privacy.md outbound-data note); docs-config drift guard green. ROADMAP #1-#5 delivered end-to-end; smaller-model rung remains gated behind `SMALLER_MODEL_ENABLED`.
